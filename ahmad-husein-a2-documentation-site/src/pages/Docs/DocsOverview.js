import styles from "./docs.module.scss";

const DocsOverview = () => {
  return (
    <>
      <h1> Welcome to the Docs</h1>
      <p>
        {" "}
        This website consists of a collection of resuable React components that
        can be employed accross a website. Their styling is intended for a
        specific project currently, the CSS can be easily modified so that it
        can suit other projects in future.
      </p>
    </>
  );
};

export default DocsOverview;
